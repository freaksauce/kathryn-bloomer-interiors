---
export interface Props {
  className?: string;
  sm: string;
  lg: string;
  alt: string;
}
const { alt, className, sm, lg } = Astro.props as Props;
---

<picture>
  <source media="(min-width: 575px)" srcset={lg} />
  <img class={className} src={sm} alt={alt} />
</picture>
